import React from 'react';
// TODO: Fase 3 — SoloView (Duplicada individual)
const SoloView: React.FC = () => (
  <div className="min-h-screen bg-pearl-50 flex items-center justify-center">
    <p className="text-stone-500">SoloView — en construcció</p>
  </div>
);
export default SoloView;
